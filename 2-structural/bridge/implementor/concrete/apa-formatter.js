"use strict";
exports.__esModule = true;
exports.APAFormatter = void 0;
var APAFormatter = /** @class */ (function () {
    function APAFormatter() {
    }
    APAFormatter.prototype.format = function (key, value) {
        return "***APA " + key + " -----" + value + " - APAFormatter";
    };
    return APAFormatter;
}());
exports.APAFormatter = APAFormatter;
